apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "monitoring.alertmanager.serviceAccountName" . | quote }}
  labels:
    {{- include "monitoring.alertmanager.labels" . | nindent 4 }}
  {{- with .Values.rbac.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end -}}
